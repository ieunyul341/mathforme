# 고1 공통수학2 · 통합과학2 30일 학습·복습 대시보드 V5

GitHub Pages에 그대로 올려 사용할 수 있는 서버 없는 정적 웹앱입니다. 기존 대시보드의 디자인과 한 장 결과물 기능을 유지하고, 날짜 자동 계산·전체 캘린더·간격 복습·오답/백지 복습·학교 복습·네 권의 교재 분량 기능을 추가했습니다.

## 바로 실행

`index.html`을 브라우저로 열어도 기본 기능을 쓸 수 있습니다. 오프라인 캐시와 PWA 설치 기능까지 확인하려면 로컬 서버 또는 GitHub Pages에서 실행하세요.

```bash
python -m http.server 8000
```

브라우저에서 `http://localhost:8000`을 엽니다.

## 기본 설정

- 학습 시작일: `2026-08-07`
- 개학일: `2026-08-12`
- 본학습: 시작일부터 30일
- 최종 복습: 마지막 학습의 +30일 복습까지
- 방학 시간: 수학 6시간 / 과학 4시간
- 개학 후 시간: 수학 3시간 / 과학 1시간 / 학교 수업 복습 1시간
- 복습 주기: +1일 / +3일 / +7일 / +14일 / +30일

상단 설정에서 시작일·개학일·시간을 바꾸면 종료일, Day 날짜, 시간표와 전체 캘린더가 자동으로 다시 계산됩니다.

## 강좌·교재

### 수학

- EBSi 정승제 공통수학2 47강
- 개념원리 공통수학2
- 마플 시너지 공통수학2

### 과학

- EBSi 김청해 `2028 수능개념 시그널 통합과학2` 40강
- 오투 통합과학2
- 완자 기출픽 통합과학2

인쇄 판본마다 페이지가 다를 수 있어 교재 과제는 단원·유형·문항 수 기준입니다. Day 상세에서 실제 푼 페이지·문제 번호를 기록할 수 있습니다.

## 주요 기능

- 시작일을 정하면 30일 본학습 종료일과 최종 복습일 자동 계산
- 방학/개학 시간표 자동 전환 및 시간 직접 수정
- 30일치 강의·교재·한 장 결과물 미션
- 전체 월간 캘린더에서 강의와 +1/+3/+7/+14/+30일 복습 미리 보기
- 날짜별 상세 화면에서 그날의 수학·과학·학교 복습·간격 복습 확인
- 복습 성공 시 다음 단계, 실패 시 오늘부터 주기 초기화
- 별도 오답 등록 및 백지 복습 등록
- 교재별 진도율, 강의 진도율, 완료일, 연속 완료일 표시
- 매일 한 장 결과물 작성·인쇄/PDF 저장
- 이미지/PDF 결과물 첨부(현재 브라우저 IndexedDB에만 저장)
- JSON 진도 백업·복원
- 다크 모드, 모바일·태블릿 반응형, 오프라인 캐시

## 데이터 저장 주의

진도·텍스트·복습 기록은 브라우저의 `localStorage`에 저장됩니다. 첨부 이미지/PDF는 `IndexedDB`에 저장됩니다. 브라우저 데이터 삭제, 시크릿 모드 종료, 기기 변경 시 사라질 수 있으므로 상단의 **진도 백업**을 정기적으로 사용하세요.

JSON 백업에는 첨부 파일 자체가 들어가지 않습니다. 첨부 원본은 별도로 보관하세요.

## GitHub Pages 배포

1. GitHub에서 새 저장소를 만듭니다.
2. 이 폴더의 파일들을 저장소 최상위에 업로드합니다. `index.html`이 루트에 있어야 합니다.
3. 저장소의 `Settings → Pages`로 이동합니다.
4. `Build and deployment → Source`에서 `Deploy from a branch`를 선택합니다.
5. Branch는 `main`, Folder는 `/(root)`로 지정하고 저장합니다.
6. 배포가 끝나면 표시되는 주소로 접속합니다.

## 파일 구조

```text
.
├── index.html
├── styles.css
├── data.js
├── app.js
├── manifest.webmanifest
├── sw.js
├── favicon.svg
├── .nojekyll
├── README.md
├── SOURCES.md
└── assets/
    ├── preview.png
    ├── preview-mobile.png
    ├── calendar-preview.png
    ├── review-preview.png
    ├── date-preview.png
    └── day-preview.png
```

## 일정 수정

강의/교재의 세부 분량을 바꾸려면 `data.js`의 `STUDY_PLAN`을 수정하세요. 기능과 화면 동작은 `app.js`, 디자인은 `styles.css`에 있습니다.
